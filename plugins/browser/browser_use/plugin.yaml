name: browser-browser-use
version: 1.0.0
description: "Browser Use (https://browser-use.com) cloud browser backend. Supports both direct BROWSER_USE_API_KEY and the managed EnergyIR tool gateway. Also powers the 'EnergyIR Subscription' UX flow that bills usage to a EnergyIR subscription."
author: NousResearch
kind: backend
provides_browser_providers:
  - browser-use
